overwrite: true
schema: "schema/graphql/civic.graphql"
# documents: "src/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
  schema/generated/graphql.schema.json:
    plugins:
      - "introspection"
