import { FlaggableOptionsDirective } from './flaggable-options.directive';

describe('FlaggableOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new FlaggableOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
