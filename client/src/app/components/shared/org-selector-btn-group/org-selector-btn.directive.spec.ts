import { OrgSelectorBtnDirective } from './org-selector-btn.directive';

describe('OrgSelectorBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new OrgSelectorBtnDirective();
    expect(directive).toBeTruthy();
  });
});
