query GeneComments(
  $geneId: Int!,
  $first: Int,
  $last: Int,
  $before: String,
  $after: String,
) {
  gene(id: $geneId) {
    id
    comments(
      first: $first,
      last: $last,
      before: $before,
      after: $after,
    ) {
      ...commentList
    }
  }
}
